# Xcode
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
*.xcodeproj/project.xcworkspace/xcshareddata/IDEWorkspaceChecks.plist
DerivedData/
build/
*.moved-aside
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
xcuserdata/
*.xccheckout
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# IDE
.idea/
*.swp
*.swo
*~

# Temporary files
*.tmp
*.temp

# Dev tools
.claude/
CLAUDE.md
*.profraw

# Documentation and assets not for repo
𓂀 ANUBIS Local LLM Testing & Benchmarking.md
anubis/anubis/README.md
anubis_icon_final.png
hf-post.md
# Anchored: only the repo-root copy is excluded. The deployed page
# leaderboard_site/anubis-oss.html IS tracked and ships via .cpanel.yml.
/anubis-oss.html
anubis - Release Steps for Auto-Update.md

# Secrets (HMAC keys, API tokens — never commit)
# NOTE: anubis/anubis/Utilities/Secrets.swift is now committed with an EMPTY
# default so the app builds out of the box. The maintainer's real secret is
# kept local via `git update-index --skip-worktree` rather than gitignore.
leaderboard_site/anubis/api/config.php
leaderboard_site/anubis/api/error_log

# Build artifacts (release zips, exported apps)
anubis/anubis-oss.zip
anubis/anubis-oss_old.zip
anubis/anubis-oss/
anubis/anubis.app/
anubis_power_logs.txt

# Refresh-findings local venv
scripts/.venv/

# Local-only planning / working docs (not pushed)
ROADMAP.md
